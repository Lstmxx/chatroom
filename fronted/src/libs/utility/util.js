import { copyToClip } from './copy.js'
import { normalizeNumber, normalizeTimeDetail, normalizeTime } from './time.js'
import { setToken, getToken } from './token.js'

const _default = {
  copyToClip,
  normalizeNumber,
  normalizeTimeDetail,
  normalizeTime,
  setToken,
  getToken
}

export default _default
